/*
Check if there is a square of 1s in a 2d matrix (that contains 0s and 1s).

The square has the size of at least sqrt(min(N,M)).

The input either has one valid square or not (all 0s).

Signature: bool hasSquare(int[][] matrix)
*/
